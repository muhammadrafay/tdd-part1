
public class Franc {
	private int amount=0;
	
	Franc(int amount) {
		this.amount= amount;
		}
	
	Franc timesReturnDollar(int multiplier) {
		//amount *= multiplier;
		//return null;
		return new Franc(amount * multiplier);
		}
	
}
